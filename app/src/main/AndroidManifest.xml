<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.spacetrader">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".views.ConfigurationActivity">
        </activity>
        <activity android:name=".views.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name=".views.PlayerIntroActivity"
                android:label="@string/title_activity_player_intro"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".views.UniverseActivity">
        </activity>
        <activity android:name=".views.MarketplaceActivity">
        </activity>
        <activity android:name=".views.StartPlayActivity">
        </activity>
        <activity android:name=".views.TravelActivity">
        </activity>
    </application>

</manifest>